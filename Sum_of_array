public int Sum(int[] vs)
        {
            int sum = 0;
            for (int i = 0; i < vs.Length; i++)
            {
                sum += vs[i];
            }
            return sum;
        }
