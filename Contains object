public bool Contains(int[] vs, int contain)
        {
            for (int i = 0; i < vs.Length; i++)
            {
                if (vs[i] == contain)
                {
                    return true;
                }
            }
            return false;
        }

